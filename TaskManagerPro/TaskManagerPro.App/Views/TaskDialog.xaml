<Window x:Class="TaskManagerPro.App.Views.TaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Add/Edit Task" Height="450" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Title" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,15"/>

        <!-- Details Grid -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Effort -->
            <TextBlock Text="Effort (Hours)" FontWeight="Bold" Grid.Row="0" Grid.Column="0"/>
            <TextBox Text="{Binding EffortHours}" Grid.Row="1" Grid.Column="0" Margin="0,0,10,10" Padding="5"/>

            <!-- Due Date -->
            <TextBlock Text="Due Date" FontWeight="Bold" Grid.Row="0" Grid.Column="1"/>
            <DatePicker SelectedDate="{Binding DueDate}" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

            <!-- Impact -->
            <TextBlock Text="Impact (1-10)" FontWeight="Bold" Grid.Row="2" Grid.Column="0"/>
            <ComboBox Grid.Row="3" Grid.Column="0" SelectedItem="{Binding ImpactScore}" Margin="0,0,10,0" Padding="5">
                <ComboBox.ItemsSource>
                    <x:Array Type="{x:Type system:Int32}" xmlns:system="clr-namespace:System;assembly=mscorlib">
                        <system:Int32>1</system:Int32>
                        <system:Int32>2</system:Int32>
                        <system:Int32>3</system:Int32>
                        <system:Int32>4</system:Int32>
                        <system:Int32>5</system:Int32>
                        <system:Int32>6</system:Int32>
                        <system:Int32>7</system:Int32>
                        <system:Int32>8</system:Int32>
                        <system:Int32>9</system:Int32>
                        <system:Int32>10</system:Int32>
                    </x:Array>
                </ComboBox.ItemsSource>
            </ComboBox>
            
            <!-- Urgency -->
            <TextBlock Text="Urgency (1-10)" FontWeight="Bold" Grid.Row="2" Grid.Column="1"/>
            <ComboBox Grid.Row="3" Grid.Column="1" SelectedItem="{Binding UrgencyScore}" Padding="5">
                <ComboBox.ItemsSource>
                    <x:Array Type="{x:Type system:Int32}" xmlns:system="clr-namespace:System;assembly=mscorlib">
                        <system:Int32>1</system:Int32>
                        <system:Int32>2</system:Int32>
                        <system:Int32>3</system:Int32>
                        <system:Int32>4</system:Int32>
                        <system:Int32>5</system:Int32>
                        <system:Int32>6</system:Int32>
                        <system:Int32>7</system:Int32>
                        <system:Int32>8</system:Int32>
                        <system:Int32>9</system:Int32>
                        <system:Int32>10</system:Int32>
                    </x:Array>
                </ComboBox.ItemsSource>
            </ComboBox>
        </Grid>

        <!-- Description -->
        <TextBlock Text="Description" FontWeight="Bold" Grid.Row="3" Margin="0,0,0,5"/>
        <TextBox Grid.Row="4" Text="{Binding Description}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Padding="5" Margin="0,0,0,15"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" IsCancel="True" Width="80" Height="30" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button Content="Save" IsDefault="True" Width="80" Height="30" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
